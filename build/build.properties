asyncqueue.build.dir = /tmp/vianetz_asyncqueue_${build.timestamp}
asyncqueue.temp.dir = /tmp/vianetz_asyncqueue

asyncqueue.git.repo = ssh://git@bitbucket.org/vianetz/magento-asyncqueue.git
; this is the branch that the package will be build from - should be master in general
asyncqueue.git.branch = develop